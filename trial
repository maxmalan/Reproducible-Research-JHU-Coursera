Prova Prova Prova
========================================


## Here I'm just doing some trials to get the correct format of the RepResearch Assignment.


```{r}
echo = TRUE
library(ggplot2)
library(dplyr)
activity <- read.csv("activity.csv", colClasses = c("integer", "Date", "factor"))
summary(activity)
str(activity)
head(activity)
tail(activity)
activity$month <- as.numeric(format(activity$date, "%m"))
```
